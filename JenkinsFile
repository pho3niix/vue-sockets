#!groovy
pipeline{
    agent any
    stages {
        stage('testing'){
            steps{
                echo "welcome to ${appname}"
            }
        }
    }
    environment{
        appName = 'flunni'
    }
}