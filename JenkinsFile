#!groovy
pipeline{
    agent any
    stages {
        stage('testing'){
            steps{
                echo "welcome to ${appname}"
                sh 'node --version'
                sh 'ls -la'
                sh 'pm2 list'
            }
        }
    }
    environment{
        appName = 'flunni'
    }
}